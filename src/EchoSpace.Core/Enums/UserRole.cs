namespace EchoSpace.Core.Enums
{
    public enum UserRole
    {
        User = 0,
        Operation = 1,  // Content moderation role (formerly Moderator)
        Admin = 2       // Highest privilege - full system access
    }
}

